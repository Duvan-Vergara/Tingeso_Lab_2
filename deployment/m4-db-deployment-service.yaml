apiVersion: v1
kind: PersistentVolume
metadata:
  name: m4-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/m4-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: m4-db-pvc
  labels:
    app: mysql
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: m4-db-deployment
  labels:
    app: m4-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: m4-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: m4-db-deployment
        tier: database
    spec:
      containers:
        - name: m4-db
          image: mysql:latest
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-root-credentials
                  key: password
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: m4-db-data
      volumes:
        - name: m4-db-data
          persistentVolumeClaim:
            claimName: m4-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: m4-db-service
  labels:
    app: m4-db-service
    tier: database
spec:
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: m4-db-deployment
    tier: database
  type: ClusterIP