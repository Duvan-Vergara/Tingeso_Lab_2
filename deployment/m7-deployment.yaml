apiVersion: apps/v1
kind: Deployment
metadata:
  name: m7-deployment
  labels:
    app: m7-deployment
spec:
  selector:
    matchLabels:
      app: m7-deployment
  replicas: 1
  template:
    metadata:
      labels:
        app: m7-deployment
    spec:
      containers:
        - name: m7
          image: duvanvergara/m7:latest
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "0"
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://$(MYSQL_HOST):$(MYSQL_PORT)/$(MYSQL_DB)
            - name: MYSQL_HOST
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: DB_HOST_M1
            - name: MYSQL_PORT
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: DB_PORT_M1
            - name: MYSQL_DB
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: DB_DBNAME_M1
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: mysql-config-map
                  key: MYSQL_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: m7-service
  labels:
    app: m7-service
spec:
  selector:
    app: m7-deployment
  ports:
    - nodePort: 30007
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort